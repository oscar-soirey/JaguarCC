using GL_FLOAT = float;
using namespace math;

class Foo;
struct Bar;

@extern
GL_FLOAT gl_test(Bar* value);

namespace math {
    int twice(int x) {
        return x * 2;
    }
}

using namespace sys;

enum Ekip {
    BONJOUR, AUREVOIR
}




void main() {
    class A {
        constr(int a) { life=a; sys:print("hello"); }
        destr() { sys:print("bye"); }
        @exposed int $ life;
    }

    A* m = new A(4);
    sys:print(m->life);
}